# Copy to .env and fill in the value from Supabase → Project Settings → Database → Connection string → URI
# IMPORTANT for Vercel: use the "Transaction" pooler (port 6543), NOT the direct connection (5432).
DATABASE_URL=postgresql://postgres.PROJECT_REF:YOUR_PASSWORD@aws-0-REGION.pooler.supabase.com:6543/postgres
